# Browser QA — 2026-09-13

Branch: v2.0.5-dev. This is alpha verification, not release approval.

## Environment

Mac Codex in-app Chromium, desktop and 390/412 px mobile viewport emulation. These are not physical iOS/Android tests. Dedicated Supabase project: INTERVAL COSMOS v2.0.5 Fresh Build Test (yaqeeyhajnpwqvdjondk). The ordinary development project remains paused. The existing test database was restored to service, not rebuilt or cleared. Fresh-build structural verifier passed: 11 courses, 13 active avatars, zero legacy tables. Issue #6 remains open for remaining integration coverage.

The isolated launcher serves test cloud configuration in memory on port 8876; it no longer overwrites cloud-config.js used by the ordinary server. Additional independent browser origins were used to exercise device sessions.

## Verified

- HISTORY mobile folders: three tabs, isolated contents, scroll to top, close/reopen reset; desktop expands all sections. Backdrop dismissal regression fixed in preceding commit 9864210.
- Guest-to-student conversion: reproduced stale guest form; fixed reload after registration and verified successful transition to START. Full-width student number normalized; duplicate number rejected.
- PIN: target waits for source approval; successful approval connects another session. Server rejects expired PIN. Added regression for source confirmation expiry when countdown element is absent; that timer fix has automated coverage, not a completed timed browser retest.
- Recovery: incorrect recovery code rejected, correct code restores test account on an independent origin.
- Assignments: expired assignments disabled; TEXT and KEYS allowed on a disposable test assignment. TEXT correct/wrong feedback, 50% achievement, later 0% attempt retaining previous achievement/best, separate mode attempt counts. Mobile result buttons previously overflowed; fixed and visually rechecked at 390 px.
- Shared history: another linked browser session displays the submitted assignment result. Local interval mastery is explicitly device-local; no claim of mastery synchronization.
- Progression: daily TEXT mission completion, FIRST SIGNAL unlock and featured badge selection, public profile showing featured badge and frame.
- Ranking: normal TEXT result initially PRIVATE, explicit publication appears on ranking; always-private preserves the two existing public best rows. Fixed misleading setting copy and removed the old base-layer hide-all call. Revised copy verified in browser.
- Student menu omits admin controls. Authenticated student database transaction rejects admin management and lacks final-delete execution privilege. This is not a complete teacher/admin browser test.
- Paused database now produces CONNECTION ERROR instead of falsely reporting missing SQL. Guest MY COSMOS explains registration instead of reporting a migration failure.

## Automated checks

24 Node test files passed, including database build, accounts, recovery, ranking, assignments, progression, PC controls, responsive HISTORY, boot errors, guest conversion and PIN expiry. Python isolated-server test passed. The test named release-blockers checks repository safeguards only; its PASS does not certify every release condition.

## Retained synthetic fixtures

QA0912 (990912), QA0913 (990913), and assignment QA0913 P1-P8 (086bd71c-9d18-497f-9e0b-ae76570853be) remain in the dedicated test project. No real student records or production database were changed. No credentials are recorded here.

## Still required

- Durable offline submission queue and reconnect/idempotency integration: queue remains unimplemented (see PRERELEASE_HARDENING). Service-worker app caching alone is insufficient.
- Complete teacher/admin permissions and destructive-operation testing in the designated test environment.
- Broader fresh-build browser matrix, assignment deadlines during gameplay, multi-device concurrent updates, and timed PIN-expiry browser retest.
- Phase 10 remaining screens and Issue #1 browser backlog; rare KEY CONFIG flicker (#7).
- Phase 11 actual artwork (explicit user request required); asset specification is preparatory only.
- Phase 12 physical Mac/Windows/iPhone/iPad/Android, browsers, online/offline matrix.

No main merge and no release declaration.
