# v2.0.5 — Next real-browser check

Latest dev changes that still require a short browser pass.

## 1. Boot / regression sanity

- student `test` starts normally
- admin `なかしま` starts normally
- SETTINGS opens/closes without freeze
- admin-only second row remains isolated; normal player layout is unchanged

## 2. PC controls

Use a desktop browser.

- answer buttons display `1 2 3 4 5 6 7 8 9 0 Q W E` key hints
- actual keys still answer the corresponding interval
- Space replays audio once (not double)
- RESULT: `R` retries
- RESULT: `Esc` returns to mode select
- typing in SETTINGS / account inputs does not trigger shortcuts
- active timed game does not exit with Esc
- active assignment game does not exit with Esc

## 3. Ranking hotfix

With an account that has a public ranking record:

- change SETTINGS to `常に非公開`
- existing public leaderboard row remains visible
- future personal-best records are not automatically published
- RESULT rank cards say `自己ベスト 月間順位` / `自己ベスト 殿堂順位`

Do not use an explicit ranking take-down action during this test.

## 4. Learning History

- intervals with fewer than 3 answers are not declared WEAK POINT
- if no interval has 3+ answers, display `判定保留`
- Learning History → weak interval → TEXT/KEYS → FOCUS transition does not flash SELECT MODE / SETTINGS

## 5. Unlock copy

On the next newly unlocked item, confirm the cut-in clearly identifies the type:

- `実績を解除しました`
- `称号を獲得しました`
- `フレームを解放しました`
- `デイリーミッション達成`

Only a naturally newly unlocked item can verify this; do not alter DB data solely for the test.

## 6. Console

Open DevTools Console after normal navigation through several screens.

- no `Multiple GoTrueClient instances` warning
- no repeating MutationObserver/freeze symptoms
- no uncaught exceptions from Phase 3/4/5/7/8 modules

## Deferred on purpose

- offline submission queue
- full SETTINGS / MY COSMOS UI redesign
- assignment gameplay layout cleanup
- assignment minimum-answer clear condition
- original art/assets
- root `supabase_setup.sql` consolidation

These are not silently considered complete.
