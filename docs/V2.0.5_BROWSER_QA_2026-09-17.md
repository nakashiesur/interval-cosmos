# Browser QA — 2026-09-17

Branch: v2.0.5-dev. Alpha verification only; main and production DB unchanged.

## Offline submission

The actual app was served through `tests/serve-offline-app.py` on 127.0.0.1:8877 against the dedicated Fresh Build Test project. A worker scoped to `/tests/offline-app/` blocked network traffic while retaining cached app files; no OS network settings were changed. This fixture tests application behavior, not the production worker's complete offline cache inventory.

- Online initialization, disconnect, reload, and offline START succeeded.
- Completed an actual 60-second TEXT game with one correct answer (80 pts).
- RESULT reported local persistence. Reloaded while still disconnected; 保存・同期 showed the same 80 pts with its original timestamp, 2026-09-17 10:10:04 JST.
- Reconnected and selected 今すぐ同期. The entry became 送信済み and remained private under the account's ask policy. Repeated sync did not add another entry.
- Read-only DB verification found one row for client event `4eb0975c-29d5-4d5e-82cb-5e142757f08a`, score 80, played_at `2026-09-17 01:10:04.551+00`, is_public false.
- Automated regression additionally covers lost acknowledgement, duplicate replay, changed publication policy, different account/device bindings, permanent rejection retention, storage failure, SDK reload after failed download, and game completion during reconnect.

The preceding 2026-09-15 DB transaction tests for the outbox and admin permissions remain in `sql/verify-offline-outbox-v205.sql` and `sql/verify-admin-permissions-v205.sql`. They rolled back their changes. Final destructive admin deletion has not been executed.

## Phase 10 UI

MY COSMOS mobile folders (概要 / 着せ替え / 実績) were implemented and checked at 390 px and desktop in the preceding session. Overview resets on reopening; desktop expands the contents. No final artwork was generated.

The latest 390 px build was rechecked for the appearance and achievement panes and the sync dialog. No horizontal overflow was observed.

At 390 px the beginner guide's title wrapped its final character onto a separate line and squeezed the back button. Adjusted its mobile header to give the labelled back button a separate row. Screenshot verification confirmed a single-line title, readable steps, all 13 reference cells, reachable completion button, and working return to practice.

## Automated checks

All 27 Node test files and the Python isolated-server test passed. `git diff --check` passed. Cache generation is alpha10-21; client asset versions were updated. Static release-blocker tests verify repository safeguards, not completion of the entire release matrix.

## Remaining release work

- Physical iPhone/iPad/Android/Windows/browser matrix, actual airplane mode and storage limits.
- Timed PIN expiry browser retest; concurrent devices; remaining teacher/admin browser and destructive test workflow.
- Remaining Phase 10 screens and Issue #1 browser checks; deferred KEY CONFIG flicker (#7).
- Phase 11 artwork awaits explicit request. Issue #6 remains open; no release approval or main merge.
- The new RPC migration is installed only on the dedicated test DB. Apply it to another authorized environment before testing this client there; the client retains failed submissions instead of discarding them.
