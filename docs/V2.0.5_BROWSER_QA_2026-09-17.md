# Browser QA — 2026-09-17

Branch: v2.0.5-dev. Alpha verification only; main and production DB unchanged.

## Offline submission

The actual app was served through `tests/serve-offline-app.py` on 127.0.0.1:8877 against the dedicated Fresh Build Test project. A worker scoped to `/tests/offline-app/` blocked network traffic while retaining cached app files; no OS network settings were changed. This fixture tests application behavior, not the production worker's complete offline cache inventory.

- Online initialization, disconnect, reload, and offline START succeeded.
- Completed an actual 60-second TEXT game with one correct answer (80 pts).
- RESULT reported local persistence. Reloaded while still disconnected; 保存・同期 showed the same 80 pts with its original timestamp, 2026-09-17 10:10:04 JST.
- Reconnected and selected 今すぐ同期. The entry became 送信済み and remained private under the account's ask policy. Repeated sync did not add another entry.
- Read-only DB verification found one row for client event `4eb0975c-29d5-4d5e-82cb-5e142757f08a`, score 80, played_at `2026-09-17 01:10:04.551+00`, is_public false.
- Automated regression additionally covers lost acknowledgement, duplicate replay, changed publication policy, different account/device bindings, permanent rejection retention, storage failure, SDK reload after failed download, and game completion during reconnect.

The preceding 2026-09-15 DB transaction tests for the outbox and admin permissions remain in `sql/verify-offline-outbox-v205.sql` and `sql/verify-admin-permissions-v205.sql`. They rolled back their changes. Final destructive admin deletion has not been executed.

## Phase 10 UI

MY COSMOS mobile folders (概要 / 着せ替え / 実績) were implemented and checked at 390 px and desktop in the preceding session. Overview resets on reopening; desktop expands the contents. No final artwork was generated.

The latest 390 px build was rechecked for the appearance and achievement panes and the sync dialog. No horizontal overflow was observed.

At 390 px the beginner guide's title wrapped its final character onto a separate line and squeezed the back button. Adjusted its mobile header to give the labelled back button a separate row. Screenshot verification confirmed a single-line title, readable steps, all 13 reference cells, reachable completion button, and working return to practice.

At 320 px, MY COSMOS truncated the player ID because the points summary shared its narrow header row. The points summary now moves below the identity at that width. The full `QA0913` ID, frame progress, and all three folders were checked in Chromium at 320 px; 390 px and desktop layouts remained intact. The narrow header has no horizontal overflow.

A new 60-second TEXT session was completed in the Fresh Build Test project at 390 px (one correct, one incorrect, 50 points). RESULT distinguished this run from the 80-point private best, labelled the best-record monthly/hall positions as `2位相当` / `3位相当`, and showed `ONLINE RECORD PRIVATE`. The public monthly TEXT ranking still contained only the published `QA0912` row; `QA0913` did not appear. RESULT controls remained reachable with no horizontal overflow.

The mobile achievements pane now groups its long list by category. A local browser fixture (`tests/cosmos-folders-browser.html`) confirmed that BASIC begins expanded, other groups begin folded, per-category unlocked counts are visible, groups can be opened independently, and a 320 px screen has no horizontal overflow. A signed-in full-app pass on the current build at 390 px confirmed the same behavior. ACCURACY expanded on tap; desktop showed all eight categories expanded; returning to mobile preserved BASIC and ACCURACY as the two open groups. No horizontal overflow was observed.

The achievement cut-in contrast backlog was reproduced with `tests/progression-cut-in-browser.html`: at 320 px the name extended beyond its unpadded border and the RESULT text showed through. The cut-in now has a bounded opaque panel, padding, and wrapping for long names. `FIRST SIGNAL` and `INTERVAL NAVIGATOR` were visually checked at 320 px, and the long name was checked at desktop width. This verifies presentation without triggering a new account achievement in the test database.

Student ASSIGNMENTS: ended tasks now start inside an expandable section so a phone with several expired tasks does not open on a long wall of disabled cards. At 390 px and 412 px, the closed group displayed a single 48 px target, expanded to all three original cards, and had no horizontal overflow. At desktop width the three cards remained expanded and accessible. Resizing an open desktop panel to 412 px collapsed the ended group. Open and upcoming tasks stay immediately visible. The teacher management screen was not changed by this folder.

## Phase 8 administrator browser pass

With explicit approval, a synthetic staff account `QA-ADMIN-0917` was registered only in the dedicated Fresh Build Test project. It began with `is_admin=false`; the test temporarily granted `is_admin=true` to that exact account and later reverted it to false. The signed-in browser showed ADMIN DASHBOARD and ADMIN ASSIGNMENTS only while the account was an admin. At 390 px the dashboard loaded three test students, student detail and management controls opened, the management overlay scrolled to its bottom, and the page had no horizontal overflow. Assignment cards and a three-student results view loaded. No student record was mutated by these browser checks.

The results view exposed raw course codes (`sound_design`, `piano`) instead of Japanese course names. The client now maps all 11 catalog codes to their display labels. A signed-in browser reload confirmed `サウンドデザインコース` and `ピアノコース` in the results view. After the final privilege revocation, a fresh browser load omitted ADMIN TOOLS, and the ordinary staff ASSIGNMENTS button opened the student missions view. The synthetic account remains non-admin in the test project; its deletion was not part of this approval. A current-build full-delete browser pass and Phase 12 integration remain outstanding.

## Ranking publication in the rank scene

Issue #1's publication choice now appears inside the rank-position cut-in when an ask-policy personal best has a top-50 virtual rank. The rank, score, monthly/hall positions, data-sharing explanation, and public/private buttons remain in one view until a choice is made. The ordinary timed cut-in still applies when no publication choice is needed; the separate privacy card remains a fallback for results without a top-50 rank.

At 390 px, a signed-in staff test account improved its TEXT best from 120 to 165 points. The actual browser showed the integrated `1位相当` / `2位相当` choice without horizontal overflow. Selecting private dismissed the cut-in, allowed the queued daily-achievement display to continue, and did not open the old separate privacy screen. Database readback showed both test sessions remained `is_public=false`. The new build also ignores command/control/option-modified keys in the app-level shortcuts; `Cmd+Shift+R` no longer starts a retry from RESULT. A stale service-worker shell initially served the previous screen; the cache generation and asset versions were bumped and the current shell was explicitly confirmed before the successful run.

The layout fixture `tests/rank-privacy-browser.html` was checked at 320 × 568 and 1280 × 800. Both choices remain visible without horizontal overflow or extra vertical scrolling; the public button no longer wraps on desktop. The integrated scene has dialog semantics and focuses the private choice. The actual browser test exercised the private path; publication and the fallback card still need a dedicated browser pass.

## Automated checks

All 28 Node test files, the Python isolated-server test, and `git diff --check` passed after ranking/privacy integration. Cache generation is alpha10-31; client asset versions were updated. GitHub Actions passed for `ace790b`; CI for ranking/privacy integration is pending push. Static release-blocker tests verify repository safeguards, not completion of the entire release matrix.

## Remaining release work

- Physical iPhone/iPad/Android/Windows/browser matrix, actual airplane mode and storage limits.
- Timed PIN expiry browser retest; concurrent devices; current-build destructive admin test workflow.
- Remaining Phase 10 screens and Issue #1 real-device/browser checks; deferred KEY CONFIG flicker (#7).
- Phase 11 artwork awaits explicit request. Issue #6 remains open; no release approval or main merge.
- The new RPC migration is installed only on the dedicated test DB. Apply it to another authorized environment before testing this client there; the client retains failed submissions instead of discarding them.
