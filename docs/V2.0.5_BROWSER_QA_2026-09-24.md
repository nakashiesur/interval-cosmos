# Browser QA — 2026-09-24

## HYPER DRIVE card contrast

The animated border was covering the card face because the shared `.mode-card::after` retained a 140 × 140 px size and its glow background. The Phase 10 override now resets the face geometry, uses an opaque dark gradient, and explicitly orders the border, face, flames, and text. Flame and time-badge positioning remains absolute. Both the MODE SELECT card and HYPER interface choices use the correction.

Verified with the actual app served by the isolated Fresh Build Test server at port 8880, using cached-account offline fallback:

- 320 × 568: MODE SELECT fits without horizontal overflow; HYPER title and time badge are visible. The HYPER interface selection retains readable TEXT/KEY descriptions and both options fit.
- 1280 × 800: MODE SELECT description is readable over the dark face; animated border and flames remain visible.
- 390 × 844: an isolated fixture made from the current app card markup retains the compact mobile layout.
- The actual app loaded foundation CSS `alpha10.8`.

The existing Phase 10 foundation and service-worker regressions passed. This was a visual check; no game score or account data was changed. A prolonged CONNECTING state was also observed before the cached-account offline fallback opened; follow-up startup resilience work is tracked separately.

## Bounded startup recovery

A slow/unreachable online service left the account gate on CONNECTING until the browser/SDK eventually failed. The gate now offers its existing recovery screen after 15 seconds. Attempt ordering and app-start guards prevent delayed results or failures from replacing a newer attempt or offline gameplay. Account identity, stored records, and publication settings are not changed by the timeout.

- Node behavior regression: immediate profile / account chooser; never-resolving initialization; timer cleanup; obsolete success and failure after retry; timeout after app start; delayed completion after offline start.
- Actual account-gate module with a deliberately delayed local cloud mock: recovery buttons displayed; at 390 × 844 the message and both controls fit without clipping; “ゲーム本体だけ起動” opened the real app START screen; “再確認” on the separate retry fixture opened PLAYER ACCESS. The mock does not contact a database.
- All 28 Node regression files and the isolated test-server regression passed.

The dedicated Fresh Build Test project was independently found INACTIVE and restoration was requested. This explains the service outage observed during the full-app visual check; it does not replace the deterministic delayed-network regression.

The dedicated project subsequently returned `ACTIVE_HEALTHY`. The current `alpha10.24` app opened successfully with its existing synthetic account and showed ONLINE. The account retained `is_admin=false`, and the main screen exposed no admin controls.

## Current-build complete deletion and revocation

With explicit approval, `QA-ADMIN-0917` temporarily received admin access in the dedicated Fresh Build Test project. A new synthetic `QA-DELETE-0924` account was created through the browser. It was initially registered as staff, then only that disposable fixture was converted to a student (unused test number `990924`, NOVA, piano) so it appeared in the student management dashboard. This setup is not evidence of the student-registration flow.

In the current `alpha10.24` browser app, the target appeared in STUDENTS → detail → MANAGE STUDENT. The irreversible confirmation button was disabled until `DELETE` was entered. The approved deletion returned to the dashboard and the target disappeared (student count 4 → 3).

DB readback confirmed zero player rows and device links for `2235b790-8fff-45b9-b57a-183a21331b7b`, and zero Auth users for its original `157f1ab8-0e63-40c6-bc5a-6aea3571e550` identity. The target had no gameplay records, so this pass does not independently prove deletion of populated history/achievement data. Reloading the deleted account's browser returned to PLAYER ACCESS.

The admin fixture was restored to `is_admin=false` immediately after deletion. Without reloading its already-open dashboard, pressing update returned `Admin account required`. After reload, the app was ONLINE and had no admin controls. No real account or production environment was changed.

## HISTORY → first-visit guide → FOCUS

The live synthetic account had fewer than three answers per interval. HISTORY correctly showed pending weakness judgement and insufficient strong/confusion data, without a weak-practice recommendation.

A further browser check exposed a navigation defect: choosing an interval and TEXT/KEYS from HISTORY reached the first-visit guide, but the requested interval/view was lost. The focus handler now retains the request across the guide and resumes after the normal guide-complete handler. Going back from the guide cancels the pending request.

Verified against `phase4-hotfix-v205.js?alpha3.4` in the current full app:

- Desktop, guide not yet completed: HISTORY → m6 → KEYS → guide → completion reached FOCUS SELECT with only m6 selected (1 / 13). Returning to PRACTICE showed the keyboard tab active.
- 390 px, guide already completed: HISTORY analysis folder → P8 → TEXT reached FOCUS SELECT with only P8 selected (1 / 13), no horizontal overflow, and no remaining transition veil. Returning to PRACTICE showed the text tab active.
- Node behavior coverage includes first-visit TEXT and KEYS, returning-user navigation, and guide cancellation. All 28 Node regression files passed.

No game was played and no additional score was submitted for these navigation checks. Physical-device transition smoothness remains part of the release matrix.

## Independent session linking and cancellation

- Explicitly approved linking QA-ADMIN-0917 from localhost:8880 to localhost:8883 in the dedicated Fresh Build Test project. PIN entry alone remained at approval waiting; source confirmation completed the link.
- Both live sessions independently loaded 3 cloud play sessions, 100% (6/6), best 225, max combo 3. Existing source remained usable. QA account remains non-admin (database read verified).
- Found target cancellation only changed the local screen without cancelling the database request. Fixed it to call the existing participant-scoped cancellation RPC, keep failures visible/retryable, and reject late results from cancelled/replaced requests. Source now keeps watching while confirmation is pending and dismisses cancelled requests. Transient dialogs clear prior countdowns.
- Actual browser local mock fixture `tests/device-link-browser.html`: successful cancellation called the RPC and returned to PIN input; `?failure=1` showed failure and retained an enabled retry button. Screenshots inspected. This fixture has no cloud client or database access.
- VM regression checks cover cancellation success/failure, stale confirmed/expired responses, normal confirmation, and source-side cancellation detection. All 28 Node test files and fresh-server Python test passed.
- Follow-up live cancellation on unlinked localhost:8884 passed: target returned to PIN input with cancellation confirmation, source automatically replaced its approval controls with cancellation notice, and DB request was cancelled with no confirmation timestamp. Reusing that PIN was rejected. The previously confirmed PIN was also rejected, but it was past its original expiry by then, so this alone does not isolate the one-time-use guarantee.

### Existing device-local analysis limitation reconfirmed

- Same cloud history summary on both sessions, but source interval analysis has six answers and new session has zero.
- `phase4-v205.js` and `phase4-analysis-hotfix-v205.js` derive interval analysis only from this origin's local mastery storage.
- Saved `interval_stats` use cumulative `mastery-v1` snapshots, with no device identity in that payload. QA snapshots contain totals of 1, 3, and 6 answers; summing would incorrectly produce 10. Selecting the newest snapshot would discard other devices' independent work.
- September 13 QA already documented mastery as device-local; this is an existing limitation, not newly lost records. Updated the HISTORY subtitle to explicitly say this device only and not synchronized. Cross-device interval analysis must not be marked passed. Historical reconstruction and future event-based aggregation need a defined migration policy before changing these user-visible statistics.

September 25 follow-up: the user approved new shared answers with separate historical local analysis. Implementation and verification are recorded in `V2.0.5_SHARED_LEARNING.md`; the limitation above describes the pre-change state.


## September 25 follow-up — assignment return routing

During shared-analysis verification, ordinary staff could start a student assignment but aborting called the module’s internal route directly, bypassing the outer admin-policy click handler. That route selected management by staff account type and received `Administrator account required`.

The internal route now selects management only by `is_admin`, including profiles fetched asynchronously. Behavioral regression covers ordinary staff/student and admin staff/student, with and without cached profiles; all 29 Node test files pass. Real-browser abort retest reached its native confirmation dialog but browser automation then stopped responding. User dismissal was requested; the final visible return remains pending. The disposable QA0913 assignment deadline was restored to its original value while waiting. No admin permission was granted.


Follow-up: the user reported no dialog was visible. Browser inventory showed no remaining tabs, so the prior request to press OK was withdrawn. A fresh test tab opened normally. `tests/assignment-return-browser.html` then exercised the real assignment engine and abort click handler with a local mock backend and an accepted-confirmation stub: PLAY → abort returned to STUDENT MISSIONS and called only `get_my_assignments`. This verifies the internal return route in a browser, but does not claim successful automation of the native confirmation or a full live-backend abort cycle.


## September 25 — ranking fallback and shortcut isolation

Added `tests/ranking-behavior-browser.html`, a local mock-backend fixture loading the production ranking, hotfix and PC-control scripts. Verified outside-top-50 ask-policy prompt (51 / 52), private continuation with zero publication calls, non-best current score 100 versus best 1,000 and explicit personal-best rank labels, and always-private changing policy without calling hide-all. These are frontend-path checks, not new live-public-ranking evidence.

Found and reproduced R triggering background retry while the publication prompt was open. Both the earlier PC handler and ranking handler now respect the foreground dialog. The fallback prompt also has dialog semantics and initially focuses private continuation. Browser retest: R leaves retry count zero; Escape closes privately without publishing; R afterwards increments retry once. The 360×640 iframe screenshot showed both choices and the explanatory text without clipping. 29 Node suites passed. Physical keyboard/browser coverage remains separate.


## September 25 — narrow-screen settings and saved-record follow-up

Real signed-in app at 360×640 iframe size: beginner guide headings, all three steps, 13-interval reference and completion button remained reachable; completion returned to the practice menu. SETTINGS controls, ranking/privacy and account/device tabs were visually inspected without clipping. No live ranking policy or account permission was changed.

Found two stale version-label writers in runtime/history extensions that replaced the current version with alpha3. Removed their ownership of presentation labels; app.js now reads the version supplied by account-gate (v2.0.4 fallback for standalone legacy loading). Repeated browser load showed alpha10.27 at both boot and SETTINGS.

The interrupted live assignment run left a 0-point TEXT result dated September 25 00:22:56 JST blocked in the 8880 test origin’s local queue after the fixture deadline was restored. The UI correctly retained it with an outside-time-window rejection. It was not deleted or resubmitted with a modified time/deadline. Translated that specific server error into an actionable Japanese start/deadline explanation; screenshot verified wrapping at 360px. The synthetic local record remains intentionally preserved. Offline regression tests and all 29 Node suites passed.


## September 25 — cache continuity and HISTORY close race

The existing linked 8883 test origin loaded alpha10.27, retained QA-ADMIN-0917, showed the original three score sessions / best 225 and shared M3 67% of three answers. Its warmed test-worker cache also booted alpha10.27 through a transport-cut reload; transport was restored and pending learning answers were zero. This is fixture-worker cache continuity, not a production service-worker upgrade or physical airplane-mode claim.

A delayed HISTORY response recreated the modal after the user closed it. Reproduced in `tests/history-lifecycle-browser.html` using the real HISTORY script with a delayed local backend. Added request generations: closing invalidates in-flight work and allows a fresh open; stale success, stale error and old analysis responses cannot render or replace the latest cache. Browser retest kept HISTORY closed after releasing the delayed response. Behavioral regression also verifies close/reopen overlap and preservation of the newer request’s loading state. All 29 Node suites passed.


## September 25 — MY COSMOS close race and cache diagnostics

Applied the same request-generation protection to MY COSMOS loads and refreshes; late responses no longer recreate closed overlays or overwrite a newer progress cache. `tests/cosmos-lifecycle-browser.html` verified close during load followed by delayed response keeps the page closed. Node regression covers overlapping open, close during refresh, and late errors; all 29 suites passed.

`tests/cache-status-browser.html` is a read-only diagnostic unless its explicit worker-update button is used; it reads worker scope/state, app cache names and three script revision URLs, never account storage. The existing 8880 app had active root Worker and only alpha10-41 app cache, containing learning-sync alpha2, HISTORY alpha3.3 and account-gate alpha5.11. This verified actual production-worker cache turnover on the dedicated local origin. Later cache revisions are expected after subsequent changes.

Console audit: the isolated direct app startup and the 8883 transport fixture had no captured warning/error entries. The 360px outer iframe preview had four `MutationObserver.observe` non-Node errors without source URLs, coinciding with wrapper reloads. The standalone root did not reproduce them. Their source is not established; do not claim the entire cross-device console audit is complete.


## September 25 — natural progression integration

Played the dedicated QA account through real UI controls without changing progression rows. HD TEXT one correct answer completed at 3,100 points (existing game scoring), remained private, unlocked HYPER IGNITION (+25), and completed HYPER SPARK (+15). The achievement cut-in was visually captured after choosing private; it did not precede the privacy choice.

A subsequent TEXT run with three correct answers scored 165 below the existing 225 best. Actual RESULT displayed current 165, best 225, explicit personal-best month/hall positions, PRIVATE and “自己ベストを維持”, with no new-best publication prompt. A further TEXT run answered five visible note pairs through UI automation (including octave subscripts), scored 800 at 5/5, and completed PRECISION CHECK. This is automated functional verification, not evidence of human response speed. While the live publication prompt was open, R had no effect; private continuation remained available.

DB readback after these plays: achievement points 350; normal/bronze/silver frames owned; HYPER SPARK and PRECISION CHECK complete; public sessions zero; is_admin false. CLEAN CONTACT was observed in the unlock queue. The short SILVER and precision-mission animations were not captured before they ended; their visual pass remains pending despite verified ownership/completion.

KEYS completed with one correct answer / 50 points and remained private. The actual KEYS ORBIT daily cut-in (+10 PT) was captured after the privacy decision. The screenshot caught its fade-in, so full-opacity contrast is not established by that capture alone. DB readback: 360 points, all three September 25 daily missions complete, seven sessions, zero public sessions, is_admin=false.

The approved linked 8883 session then displayed all seven sessions including the four new plays, the new shared interval counts, and the preserved M3 confusion. MY COSMOS displayed all three missions complete and SILVER equipped. Closing and reopening retained 360 points without another unlock animation. Its transport fixture remained connected with zero pending learning answers. The fixture's OFFLINE label after a transport-only reconnect reflects that the fixture does not dispatch a browser online event; this observation is not evidence of a physical network reconnect failure.
