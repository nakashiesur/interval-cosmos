# September 26 — Student account integration

Dedicated Fresh Build Test project only; v2.0.5-dev. User approved a synthetic student and personally entered the recovery credential / submitted registration.

- QA-STUDENT-0926 registered as student, non-admin, ask-before-publication. Recovery configuration exists; no recovery value or hash was read.
- Initial MY COSMOS: 0 points, NORMAL, no unlocked achievements. Student menu had no admin controls.
- Real TEXT play: two correct answers (m2, P5), 105 points. RESULT asked before publication; chose to keep private. FIRST SIGNAL appeared after that choice.
- DB readback: exactly one saved session, zero public sessions, two learning-answer events, non-admin unchanged.
- A second tab at the same origin initially retained its pre-registration screen; reload restored the registered student normally. Opening HISTORY after the play ended showed one session, 105 best, 2/2 correct. Both shared interval cards showed one correct answer, and weakness/strength remained pending for insufficient data.
- Same-origin tabs are not an independent-device test. A separate-origin target at port 8885 submitted the source PIN and reached approval pending. Source displayed the explicit approval/rejection controls. No new device has been approved yet; final additional-access confirmation requested.

Remaining: approved independent-session connection, one-time PIN reuse/expiry, student recovery with retained history, guest migration, physical-device matrix. No production or main-branch changes; no public ranking changes.

## Independent student sessions and simultaneous play

User completed the pending source approval. Port 8885 opened the registered student, showed the original 105-point session and both shared answer counts. Original port 8884 remained signed in; DB confirmed exactly two devices and no admin rights.

Started actual TEXT games on both origins less than a second apart. Source answered M2, target answered m2; each finished at 50 points. Both result screens retained the previous best 105 and stayed private without a best-update publication prompt. DB readback: precisely three sessions (105/50/50), four learning answers, zero public sessions, and 20 progression points. Target HISTORY matched three plays / four correct answers, with m2 two answers, M2 one, P5 one. This is isolated browser-session evidence, not physical phone + PC coverage.

A third unlinked origin rejected reuse of the consumed PIN. The rejection exposed an English-only message; replaced it with Japanese directions to check the number or request a new PIN from the original device. Real-browser repeat verified the new copy. Recovery screens also carried an obsolete app version; now their footer reads the current app version at render time. All 29 Node suites pass. Actual credential recovery is awaiting user entry of the existing code; no code or hash has been inspected.

## Recovery and student offline replay

User submitted the existing recovery code in the third origin (8886). The restored account displayed all three sessions / four correct answers / best 105, with the shared interval counts intact. Database confirmed three devices, 20 points, non-admin, no public sessions. The original 8884 session reloaded successfully and still displayed its profile and 20 points. No credential values were inspected.

In 8885, a test-scope service worker cut transport while preserving the real application and dedicated backend. A real TEXT play (m6 correct, 50 points) finished with a local-save notice. Offline reload retained both the pending score and one pending answer. After transport restoration and manual sync, both cleared; repeating sync left exactly four total sessions / five answers and zero public scores, with original played_at retained (2026-09-26 06:22:15 UTC). This simulates transport failure; physical airplane mode is still unverified.

A local mock-backed browser fixture exposed a separate sync-dialog race: a delayed policy response entered a newly reopened dialog. The UI now associates each async action with its original dialog, ignores stale successes/errors, and stops a closed pre-send policy check before resending. Closing also releases its busy state; an older completion cannot release a newer request. Browser close/reopen/release retest passed with the current versioned script. Added behavioral Node regression and CI coverage.

## Student assignment integration

Created one reversible synthetic assignment directly in the dedicated database (not through the teacher creation UI): `QA0926 Student Integration`, TEXT / KEYS, P1 / P8, 50% accuracy target, 30-minute deadline. No admin privileges were granted.

- Initial TEXT run elapsed without an answer and saved a not-reached result. Retry answered P8 and P1 correctly: 135 points, 100%, achieved, two attempts.
- Independent source session played KEYS and correctly answered the visibly highlighted octave: 50 points, 100%, achieved. Its assignment list showed both mode-specific scores from the two origins.
- A third TEXT attempt deliberately answered P1 for an octave, then elapsed. Result showed THIS RUN 0 / 0%, TARGET ALREADY ACHIEVED, and explicitly stated that this run missed the target while the previous best was retained. Screenshot verified the distinction.
- Database readback retained TEXT best 135 / 100%, three attempts, achieved; KEYS best 50 / 100%, one attempt, achieved. Source browser error/warning log was empty at inspection.
- Unpublished only this synthetic assignment after the test; retained its records for evidence. Older expired assignments and the older staff account's rejected pending assignment were untouched.

This passes student multi-mode / retry / best / prior-achievement behavior on isolated browser sessions. It does not prove teacher creation, exact deadline boundaries, or physical-device behavior.

## Populated account deletion

User explicitly approved complete deletion of QA-STUDENT-0926 and its test records / three linked sessions. Temporarily enabled the previously authorized QA-ADMIN-0917 fixture, then used current alpha10.27 browser ADMIN DASHBOARD → student detail → MANAGE STUDENT → complete deletion, typing the required DELETE confirmation.

Before deletion: 8 play sessions, 9 learning answers, 2 achievements, 1 assignment best, 2 mode bests, 3 daily progress rows, 3 devices, and one each of device-link request, frame, recovery credential, title, and public profile; 2 ranking bests. No secret credential values were read.

After deletion: player absent; all 13 related player_id tables contained zero rows for the target; all three original Auth user IDs were absent. Dashboard removed the target and retained the other three students. Admin permission was immediately revoked; updating the still-open dashboard was denied with Admin account required. Original and recovered origins (8884 / 8886) returned to PLAYER ACCESS after an online reload.

Local development servers from the prior turn had stopped; restarted the dedicated-only servers before final browser checks. The old offline fixture tab encountered a browser error-page navigation restriction; a new tab at the same 8885 origin returned to PLAYER ACCESS, completing all three origin checks. Admin reload had no management controls. No production or main changes.

## Deleted identity followed by guest registration

Guest at 8886 completed TEXT 115 / 2 correct (m6, TT), with each local mastery card at 15%. User then submitted QA-GUEST-0926 registration and hit player_devices_auth_user_id_fkey. No player row was created for 99092602. Root cause: getSession returned a cached JWT for the deleted Auth identity; that token can remain usable until expiry even though no Auth row exists.

Added online-only ensureAccountAuth for registration and recovery. It validates with getUser and replaces the local session only on explicit user_not_found/session_not_found; network/server errors preserve the session. Ordinary offline play is unchanged. Recovery-enabled registration and fallback registration both use the guard. A same-origin browser diagnostic using the real cloud module replaced the deleted identity successfully without reading credentials or submitting registration. All 31 Node suites passed, including missing-user replacement and non-destructive transient-error cases. User registration resubmission remains required because entering/submitting new recovery credentials is a user handoff.

## Guest conversion completed and local analysis visibility

User resubmitted the prepared registration successfully: QA-GUEST-0926, student 99092602, non-admin, ask policy. HISTORY correctly starts with zero online sessions and shared answers. Guest TEXT 115 / m6 + TT was stored in the existing device mastery, but the registered history displayed only shared analysis and the older frozen archive, making those guest answers inaccessible there.

Added a collapsed current-device analysis panel, explicitly including guest answers and explaining that it is browser-wide, overlaps shared answers, and is never added to account totals. The frozen pre-sync archive remains separate. Real registered browser displayed m6 and TT at 1 answer / 100% each, while shared analysis remained zero. Behavioral regression verifies both isolation and that guest HISTORY itself does not duplicate the device panel. 32 Node suites pass. Guest scores are not retroactively published or imported into online sessions.
