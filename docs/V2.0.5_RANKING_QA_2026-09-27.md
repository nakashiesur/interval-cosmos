# Ranking decision timing QA — 2026-09-27

- Reproduced both in behavioral regression and a browser fixture: a quick private decision closes the integrated rank scene, but the delayed fallback timer reopens the separate publication prompt. The same stale timer could reopen it after successful publication.
- Delayed fallback now checks current publication_required as well as session identity. Both decisions remain final for that presentation.
- During an in-flight publication, both choice buttons are disabled and Escape cannot falsely announce that the dispatched request was kept private. Failure restores both choices; successful completion closes the presentation.
- Actual browser fixture, using real phase3 script and mock cloud only: private→delayed timer PASS; public→delayed timer PASS; 10-second delayed publication→Escape retained pending dialog; completion closed it; simulated failure restored buttons and private selection worked.
- No actual ranking record or account was published or changed by this test.
- All 35 Node suites passed, including the structural release gate. This is not evidence of complete real-device release readiness.
- Cache alpha10-57, phase3 asset alpha3.9. Issue #13 still covers broader final visual acceptance; this patch fixes timing without changing public/private policy.
