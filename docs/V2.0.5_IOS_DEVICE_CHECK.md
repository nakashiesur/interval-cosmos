# iPhone / iPad first physical-device pass

Same-Wi-Fi preview: start `python3 scripts/serve-device-test.py --host <Mac LAN IP> --port 8896` and open that HTTP address in Safari. Keep Mac awake. This server only exposes assets listed in sw.js and dedicated Fresh Build Test configuration; repository metadata, SQL, work notes, tests and directory listings return 404. Default host stays loopback. No router forwarding or external publication.

This HTTP preview covers layout, touch and audio. Service workers require a secure context on iOS, so it is NOT an offline/PWA/installation acceptance environment. A trusted HTTPS test deployment is required for that later pass. Never bypass browser certificate warnings.

First user pass on each device:
1. Open app and choose guest. Do not enter a real student number or recovery credential.
2. Start PRACTICE, replay sound, answer several questions; report silent, delayed or interrupted audio.
3. Open settings and learning history; verify touch controls, vertical scrolling, close buttons and portrait/landscape layout.
4. Report device model, iOS/iPadOS version and the screen/action where any issue appeared. No account secrets are needed.

Registered MY COSMOS / artwork and real offline/multiple-device tests follow after connectivity/audio pass and secure test URL setup. No production deployment or main merge is implied.

## User-reported physical-device evidence — 2026-09-27

The user explicitly confirmed **both iPhone and iPad** opened the same-Wi-Fi test app, played sound and accepted controls. This establishes basic physical Safari connectivity/audio/touch operation. Device models and OS versions were not provided. It does not establish registered-account sync, complete game results, history persistence, landscape layout, offline recovery or PWA installation.

Next physical pass: complete one guest TEXT game on each device, open learning history and verify that play appears; rotate portrait/landscape and confirm visible controls and close action. Keep online for this HTTP preview.

## Physical-device feedback and fixes

User tested iPhone 12 Pro Max and 13-inch M4 iPad: both completed TEXT but showed no session history, reported landscape clipping, and RESULT showed PC key labels. Attached landscape screenshot shows the question consuming the viewport with answers below it.

- Root cause of history: guest route deliberately passed an empty session list, and completed guest sessions were never saved. Added local-only latest-500 session storage and guest history reads, with no upload or attribution to a registered account. Previously unsaved session details cannot be reconstructed from aggregate mastery.
- Landscape up to 900px viewport height uses question/answers side by side. Short phone landscape flattens answers into four columns; taller landscape retains interval-family rows. Touch/coarse/no-hover RESULT hides PC hint spans.
- Full app browser: completed guest TEXT (50 points, 1/1), HISTORY showed one session, and reload retained it. 926×320 landscape showed all 13 answer buttons; 428px RESULT omitted R/ESC text. iOS/iPadOS retest remains required; viewport emulation does not prove Safari capability detection.
- Regression: guest persistence/reload/bounds/storage failure and guest/account separation; 36 Node suites and restricted-device server tests PASS. App alpha10.30/cache alpha10-59.

## User-confirmed retest — 2026-09-27

On **both iPhone 12 Pro Max and 13-inch M4 iPad**, the user confirmed after the fix:
- Completed TEXT appears in learning history.
- All answer buttons are visible in landscape; no reported layout problem.
- RESULT no longer displays R / ESC hints.

These three reported defects pass physical-device retest on these devices. Other modes, offline/PWA and registered multi-device sync are not implied by this pass.

## KEYS / EAR follow-up
- Desktop browser at 926×320: KEYS displayed the entire keyboard, replay/audio controls, all 13 answers and hold-to-end control. A timed-out KEYS session reached RESULT.
- EAR initially pushed hold-to-end below the viewport because the listening animation/text and concealed note block together exceeded the question area. Added short-landscape-only sizing for those elements. Same-size real browser screenshot now includes all controls. Other viewport heights retain their existing EAR presentation.
- Cache alpha10-60; 36 Node suites PASS. Physical KEYS/EAR audio and touch verification is the next user check.

## 2026-09-27 portrait follow-up

User confirmed audio, replay and reachable hold-to-end work on both physical devices; landscape is good. Portrait KEYS on iPad hid the exit control; EAR on iPad clipped answers and exit, and EAR on iPhone hid exit.

Adjusted portrait-only viewport budgeting: single-row HUD, compact question/EAR presentation, capped keyboard height, viewport-scaled seven answer rows (minimum 44px), explicit 44px exit control. No clipping/overflow hiding was introduced. Cache alpha10-61, device fixes CSS v5.

Browser checks: 428×700 EAR exit bottom 672px; KEYS 665px. At 1024×1100 EAR exit bottom 989px, KEYS 1018px. Screenshots confirm all 13 answers, replay, keyboard (KEYS), and exit visible. Landscape KEYS 926×320 remains visible (exit bottom 316px). These are browser viewport checks, not Safari hardware acceptance. Physical portrait retest pending. All 36 Node regression suites pass.
